server {
    listen 8080;
    server_name localhost;

    location / {
        # Передаем трафик в Unix-сокет Gunicorn
        proxy_pass http://unix:/tmp/app.sock;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}